version: '3'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/code
    environment:
      - FLASK_APP=./src/app.py
      - FLASK_RUN_HOST=0.0.0.0
  redis:
    image: "redis:alpine"